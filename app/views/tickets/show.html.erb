<div id='ticket'>
  <h2><%= @ticket.title %></h2>
  <%= link_to "Edit Ticket", [:edit, @project, @ticket] %>
  <%= simple_format(@ticket.description) %>
  <! converts line breaks so the description renders exactly as
  the user intends it to. !>
</div>
